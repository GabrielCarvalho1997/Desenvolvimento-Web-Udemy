const num1 = window.prompt('Digite um número.');
const num2 = window.prompt('Digite outro número.');
const resultado = parseFloat(num1) + parseFloat(num2)

window.alert(`O resultado da soma dos número é ${resultado}`)